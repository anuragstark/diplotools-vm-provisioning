# Let's search for "grafana nginx subpath redirect loop" online or in docs if possible,
# but since I have a bash terminal I can just google it via curl or python if needed.
# Actually I know the answer:
